<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8"> <!-- Especifica la codificación de caracteres del documento -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Configuración para la vista adaptable -->
    <title>Microsoft Azure</title> <!-- Título de la página -->
    <link rel="stylesheet" href="resources/css/style.css"> <!-- Enlace a la hoja de estilos CSS -->
    <link rel="icon" href="resources/img/Microsoft_Azure.svg,png"> <!-- Favicon de la página -->
    
    <!-- Enlace a Bootstrap CSS desde CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    
    <!-- Preconexión a Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    
    <!-- Enlace a Google Fonts para la fuente "League Spartan" -->
    <link href="https://fonts.googleapis.com/css2?family=League+Spartan:wght@100..900&display=swap" rel="stylesheet">
</head>

<body class="bg-dark"> <!-- Clase de Bootstrap para fondo oscuro -->
    <section class="container"> <!-- Contenedor principal -->
        <div class="row g-0"> <!-- Fila sin espacios (gutter) -->
        </div>
        
        <!-- Columna con el formulario de registro -->
        <div class="register-form col-lg-5 d-flex flex-column align-items-center justify-content-center min-vh-100">
            <div class="pt-lg-2 pb-lg-3 p-4">
                <div class="logo-azure w-100">
                    <img src="resources/img/Microsoft_Azure.svg.png" class="img-fluid"> <!-- Logo de Azure -->
                </div>
            </div>
            <div class="px-lg-5 py-lg-4 p-4 w-100">
                <h1 class="font-weight-bold nb-4">Hola, Registrate en AZURE</h1>
                <p class="d-inline-block mb-0">Por favor ingresa tus datos</p>
                <form action="/register" method="POST" class="nb-5"> <!-- Formulario de registro -->
                    <!-- Campo de usuario -->
                    <div class="mb-3">
                        <label for="user" class="form-label">Usuario</label>
                        <input type="text" name="user" class="form-control bg-dark-x border-0 font-weight-bold"
                            placeholder="Ingresa tu usuario" id="user" aria-describedby="Usuario" required>
                    </div>
                    <!-- Campo de nombre -->
                    <div class="mb-3">
                        <label for="name" class="form-label">Nombre</label>
                        <input type="text" name="name" class="form-control bg-dark-x border-0 font-weight-bold"
                            placeholder="Ingresa tu Nombre" id="name" aria-describedby="name" required>
                    </div>
                    <!-- Campo de rol -->
                    <div class="mb-3">
                        <label for="rol" class="form-label">Rol</label>
                        <input type="text" name="rol" class="form-control bg-dark-x border-0 font-weight-bold"
                            placeholder="Ingresa tu Rol" id="rol" aria-describedby="rol" required>
                    </div>
                    <!-- Campo de contraseña -->
                    <div class="mb-3">
                        <label for="pass" class="form-label font-weight-bold">Contraseña</label>
                        <input type="password" name="pass" class="form-control bg-dark-x border-0 font-weight-bold"
                            placeholder="Ingresa tu contraseña" id="pass" required>
                    </div>
                    <!-- Botones de cancelar y guardar -->
                    <div class="d-flex justify-content-around">
                        <a href="/" type="submit" class="btn btn-cancel btn-outline-light flex-grow-1 mx-2">CANCELAR</a>
                        <button type="submit" value="save" class="btn btn-outline-light flex-grow-1">GUARDAR</button>
                    </div>
                </form>
            </div>
        </div>
    </section>

    <!-- Enlace a SweetAlert2 desde CDN -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <!-- Script para mostrar alertas de SweetAlert2 si existe una alerta definida -->
    <% if(typeof alert !="undefined" ) { %>
        <script>
            Swal.fire({
                title: '<%= alertTitle %>',
                text: '<%= alertMessage %>',
                icon: '<%= alertIcon %>',
                showConfirmButton: <%= showConfirmButton %>,
                timer: <%= timer %>
            }).then(() => {
                    window.location = '/<%= ruta %>'
                })
    <% } %>
        </script>

    <!-- Enlace a Bootstrap JS desde CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>
